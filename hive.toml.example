# Copy to ~/.config/hive/hive.toml (or run `hive init`).
# Hosts are opt-in. Hive never scrapes every Host in ~/.ssh/config.

prefix = "ctrl-space"

[[hosts]]
id = "local"
label = "local"
local = true
tmux = "tmux"

# [[hosts]]
# id = "devbox"
# ssh = "devbox"
# label = "devbox"
# tmux = "tmux"
# # socket = ""
# # control_path = "~/.ssh/hive-devbox.sock"
